# Production deployment URL (no trailing slash).
# Set this before launching so SEO metadata, canonical URLs,
# the sitemap and robots.txt reference the real domain.
NEXT_PUBLIC_SITE_URL=https://your-domain.com